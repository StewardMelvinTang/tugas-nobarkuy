<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ptsganjil202111rpl1melvin30">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="NobarKuy"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ptsganjil202111rpl1melvin30">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".MovieDetail" />
        <activity android:name=".AllMovies" />
        <activity android:name=".MainActivity" />
        <activity android:name="Other.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>